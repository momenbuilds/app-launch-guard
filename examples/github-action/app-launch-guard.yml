name: AppLaunchGuard

on:
  pull_request:
  push:
    branches: [main]

jobs:
  app-launch-guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: momenbuilds/app-launch-guard@v1
        with:
          path: "."
          output: "markdown"
          fail-on: "critical"
          no-color: "true"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: app-launch-guard-report
          path: app-launch-guard-report.md
